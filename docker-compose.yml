version: '3.8'

services:
  kpi_hub:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kpi_hub_app
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
